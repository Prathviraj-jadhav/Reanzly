# Fail2ban Filter for Caddy Security Audit Logs (SQLi, XSS, Scrapers)
[Definition]
failregex = ^.*"remote_ip":"<HOST>".*"(Forbidden: Security Violation|Automated Security Scanner Blocked)".*$
ignoreregex =
