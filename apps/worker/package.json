{
  "name": "@reanzly/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@reanzly/database": "0.0.0",
    "@reanzly/shared": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.20.3",
    "typescript": "^5"
  }
}
